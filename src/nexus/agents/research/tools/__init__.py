"""Research tools (arxiv, tavily, wikipedia, semantic scholar, fetch) and per-role allowlist plumbing."""

from .tools import *  # noqa: F401,F403 - re-export so callers can still do `from ..tools import tools`
